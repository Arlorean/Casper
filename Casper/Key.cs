using System;
using System.Collections.Generic;
using System.Text;

namespace Casper {
    public enum Key {
        // Left hand side
        CAPS, Z, X, C, V,
        A, S, D, F, G,
        Q, W, E, R, T,
        D1, D2, D3, D4, D5,
        // Right hand side
        D0, D9, D8, D7, D6,
        P, O, I, U, Y,
        ENTER, L, K, J, H,
        SPACE, SYMB, M, N, B,  
    }
}
